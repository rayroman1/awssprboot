# yaml stands for (Yaml Ain't Markup Language')
server:
  port: 5000
  
spring:
  datasource:
    url: jdbc:postgresql://sept-2109-java.c7otdgsixoyh.us-east-2.rds.amazonaws.com:5432/devopsdb
    username: devopsuser
    password: P4ssw0rd
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: update
